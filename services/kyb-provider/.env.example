# Server
PORT=3001

# When true, POST /kyb is paywalled via x402.
X402_ENABLED=false

# x402 settings
# Network must be one of: base-sepolia, base, avalanche-fuji, etc (see x402 NetworkSchema).
X402_NETWORK=base-sepolia

# Recipient address for payments (your wallet address on the chosen network).
# NOTE: this MUST be an address (0x + 40 hex chars), not a private key.
X402_PAY_TO=0x0000000000000000000000000000000000000000

# RPC used by local x402 verify/settle facilitator logic.
# For Anvil fork tests, use http://127.0.0.1:8545
X402_RPC_URL=

# Relayer key used to submit transferWithAuthorization in settle.
# If omitted, PRIVATE_KEY is used.
X402_RELAYER_PRIVATE_KEY=

# Optional EIP-712 domain override for assets where library defaults mismatch onchain metadata.
# If unset, server tries to read `name()` and `version()` from the asset contract.
X402_EIP712_NAME=
X402_EIP712_VERSION=

# Price in USD (converted to USDC atomic amount by x402 helpers; 0.01 => 10000 atoms at 6 decimals).
KYB_PRICE=0.01

# Max time (seconds) a payment is considered valid.
X402_TIMEOUT_SECONDS=120

# --- Sumsub (Sandbox) ---
# Create Sandbox keys in Sumsub and put them here.
SUMSUB_BASE_URL=https://api.sumsub.com
SUMSUB_APP_TOKEN=
SUMSUB_SECRET_KEY=

# Business verification level name from your Sumsub dashboard (sandbox).
SUMSUB_LEVEL_NAME=

# --- Document resolver (IPFS/HTTPS bundle ingestion) ---
# Optional shared secret required by POST /docs/resolve as `x-doc-resolver-key`.
DOC_RESOLVER_API_KEY=

# Gateway used to resolve ipfs:// URIs.
DOC_RESOLVER_IPFS_GATEWAY=https://ipfs.io/ipfs

# Allow http:// metadata URIs (for local-only testing).
DOC_RESOLVER_ALLOW_INSECURE_HTTP=false

# Optional host allowlist (comma-separated, lower-case host[:port]).
DOC_RESOLVER_ALLOWED_HOSTS=

# Fetch guardrails.
DOC_RESOLVER_TIMEOUT_MS=15000
DOC_RESOLVER_MAX_BYTES=2000000
